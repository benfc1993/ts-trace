{
  "name": "func-mapper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "cp plugin/viewer/index.html dist/index.html && esbuild plugin/viewer/index.ts --bundle --watch --outdir=dist --minify --servedir=dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.12.12",
    "esbuild": "^0.21.3",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "ts-morph": "^22.0.0"
  }
}
